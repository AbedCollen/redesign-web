import { MonacoEditor } from '@components/custom';

const ApplicationsManifest = () => {
  return <MonacoEditor language="JSON" />;
};

ApplicationsManifest.displayName = 'ApplicationsManifest';

export default ApplicationsManifest;
